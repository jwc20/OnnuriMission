---
import Footer from "../components/Footer.astro";

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!-- 
<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Onnuri Church" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/onnuri_new-logo_V5.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
    </head>
    <body>
        <slot />
        <Footer />
    </body>
</html>

<style is:global>
    html {
        font-family: system-ui, sans-serif;
        background: white;
        background-size: 224px;
    }

    body {
        margin: 0;
        padding: 5rem;
        font-size: 16px; /* A reasonable default font size */
    }

    /* Minimum size */
    @media screen and (max-width: 360px) {
        body {
            font-size: 5px; /* Avoid fonts becoming too small on tiny screens */
        }
    }
    img {
        max-width: 100%;
        height: auto;
    }

    code {
        font-family:
            Menlo,
            Monaco,
            Lucida Console,
            Liberation Mono,
            DejaVu Sans Mono,
            Bitstream Vera Sans Mono,
            Courier New,
            monospace;
    }
</style> --><!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Onnuri Church" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/onnuri_new-logo_V5.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
    </head>
    <body>
        <slot />
        <div class="footer"><Footer /></div>
    </body>
</html>

<style is:global>
    html {
        font-family: "Noto Sans", sans-serif;
        background: #f5f5f5;
        background-size: cover;
        color: #333;
    }

    body {
        margin: auto;
        /* padding: 3rem; */
        font-size: 18px;
    }

    /* @media screen and (max-width: 768px) {
        body {
            margin: auto;
            padding: 2rem 1rem;
            font-size: 16px;
        }
    } */

    img {
        max-width: 100%;
        height: auto;
        /* center */
        display: block;
        margin: 0 auto;
    }

    .footer {
        font-size: 12px;
        text-align: center;
        margin-top: 2rem;
        color: rgba(0, 0, 0, 0.4);
        a {
            color: rgba(0, 0, 0, 0.4);
        }
    }

    code {
        font-family: "Fira Code", monospace;
    }
</style>
